<div>
  <%= render @post %>

  <div id="comments" class="flex flex-col p-4 gap-4">
    <%= render @post.comments %>
    <%= render "comments/form", comment: @post.comments.build(author: Current.user) %>
  </div>
</div>
